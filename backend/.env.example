# ===========================================
# Production Environment Variables for Render
# ===========================================

# REQUIRED - Generate a secure random key (32+ chars)
# Example: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=81a5a519ebebb28fd34772c4ac04a12d7991b39648f9f90c45e84704457d7dce

# Super Admin Credentials (set before first deploy)
SUPER_ADMIN_USERNAME=SuperAdmin
SUPER_ADMIN_PASSWORD=SuperAdmin

# Frontend URL for CORS (your Vercel deployment)
FRONTEND_URL=https://gcee-examhall.vercel.app

# Database (auto-set by Render when you link PostgreSQL)
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# Flask Environment
FLASK_ENV=production

# ===========================================
# Rate Limiting Configuration (adjust as needed)
# ===========================================
# Default: 200 requests/day, 50 requests/hour per IP
# For exam allocation system:
# - Admins: ~50 actions/session (upload, generate, download)
# - Students: ~5-10 searches/day per student
# Current limits are reasonable for this use case
